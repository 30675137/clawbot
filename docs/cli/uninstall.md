---
summary: "CLI reference for `clawbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `clawbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
clawbot uninstall
clawbot uninstall --all --yes
clawbot uninstall --dry-run
```

